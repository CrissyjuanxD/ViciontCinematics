# Configuración de Viciont Cinematics
# ====================================

# Configuración de efectos visuales
effects:
  # Unicode para el efecto fade (pantalla negra)
  fade_unicode: "\uEAA8"

  # Fade inicial (al empezar cinemática)
  initial:
    fade_in_duration: 40      # Fade in inicial
    fade_stay_duration: 40    # Stay inicial (TP al final del stay)
    fade_out_duration: 40     # Fade out inicial

  # Fade final (al terminar cinemática)
  final:
    fade_in_duration: 20      # Fade in final
    fade_stay_duration: 60    # Stay final (TP durante el stay)
    fade_out_duration: 40     # Fade out final

# Configuración de reproducción
playback:
  # Sistema de interpolación de frames
  interpolation:
    # Habilitar interpolación (SIEMPRE habilitado para 60 FPS optimizado)
    enabled: true
    # FPS fijo en 60 para máxima suavidad y fluidez
    target_fps: 60
    # Tipo SMOOTH optimizado para rotaciones ultra suaves
    type: "SMOOTH"
    # Suavizado avanzado de rotaciones (elimina giros bruscos)
    rotation_smoothing: true
    # Factor optimizado para rotaciones cinematográficas
    smoothing_factor: 0.25

  # Sistema legacy (para compatibilidad)
  legacy:
    # Usar sistema antiguo en lugar de interpolación
    use_legacy_system: false
    # FPS del sistema legacy
    fps: 20
    # Usar frames prolongados (solo sistema legacy)
    use_prolonged_frames: false

# Configuración de sonidos
sounds:
  # Habilitar sistema de sonidos
  enabled: false
  # Volumen por defecto (0.0 - 1.0)
  default_volume: 1.0
  # Pitch por defecto (0.5 - 2.0)
  default_pitch: 1.0
  # Sonidos predefinidos para usar en cinemáticas
  presets:
    # Sonidos de Minecraft nativos
    pling: "minecraft:block.note_block.pling"
    bell: "minecraft:block.note_block.bell"
    chime: "minecraft:block.note_block.chime"
    harp: "minecraft:block.note_block.harp"
    bass: "minecraft:block.note_block.bass"
    # Sonidos de transición
    transition_tp: "minecraft:entity.enderman.teleport"
    transition_whoosh: "minecraft:item.elytra.flying"
    # Sonidos ambientales
    ambient_cave: "minecraft:ambient.cave"
    ambient_wind: "minecraft:ambient.basalt_deltas.mood"
    # Sonidos personalizados (ejemplos)
    custom_wait1: "minecraft:custom.espera1"
    custom_transition: "dtools3:tools.transitions.tp"
    custom_dramatic: "resourcepack:cinematic.dramatic"

# Configuración general
general:
  # Silencio global durante cinemáticas
  global_mute: false
  # NO ocultar jugadores - mantenerlos visibles con packets
  auto_hide_players: false
  # Restaurar ubicación original
  restore_location: true
  # Restaurar modo de juego original
  restore_gamemode: true

# Optimizaciones para 100+ jugadores
performance:
  # Usar packets de ProtocolLib para optimización extrema
  use_packets: true
  # Mantener jugadores visibles durante cinemáticas
  keep_players_visible: true
  # Pool de objetos Location para reducir garbage collection
  location_pool_size: 1000
  # Procesamiento asíncrono (mejora TPS con muchos jugadores)
  async_frame_processing: true
  # Batch size para teleports (optimiza red con muchos jugadores)
  teleport_batch_size: 50